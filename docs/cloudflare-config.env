# ========================================
# CONFIGURACIÓN PARA CLOUDFLARE TUNNEL
# ========================================
# URL: https://occupation-belong-costa-jerry.trycloudflare.com
#
# INSTRUCCIONES:
# 1. Copia las líneas de configuración que necesites
# 2. Pégalas en tu archivo .env (reemplazando los valores locales)
# 3. Ejecuta: php artisan config:clear && npm run build
# 4. Reinicia tus servicios
# ========================================

# Aplicación
APP_ENV=production
APP_DEBUG=false
APP_URL=https://occupation-belong-costa-jerry.trycloudflare.com

# Reverb WebSocket - Servidor (interno)
REVERB_SERVER_HOST=0.0.0.0
REVERB_SERVER_PORT=8080

# Reverb WebSocket - Cliente (público - para el navegador)
REVERB_HOST="occupation-belong-costa-jerry.trycloudflare.com"
REVERB_PORT=443
REVERB_SCHEME=https

# Variables para el frontend (Vite) - SE COMPILAN EN EL JAVASCRIPT
# IMPORTANTE: Después de cambiar estas, ejecutar: npm run build
VITE_REVERB_HOST="occupation-belong-costa-jerry.trycloudflare.com"
VITE_REVERB_PORT=443
VITE_REVERB_SCHEME=https

# Sesiones (importante para evitar error 419)
SESSION_DOMAIN=occupation-belong-costa-jerry.trycloudflare.com
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=none

# ========================================
# COMANDOS A EJECUTAR DESPUÉS DE CAMBIAR:
# ========================================
# php artisan config:clear
# php artisan route:clear
# php artisan view:clear
# npm run build
#
# Si usas el script automatizado:
# .\start-dev-cloudflare.ps1
