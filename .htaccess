<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige todas las peticiones a index.php si el archivo/directorio no existe
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>